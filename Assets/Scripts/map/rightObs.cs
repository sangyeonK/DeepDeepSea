using System.Collections; using System.Collections.Generic; using UnityEngine;  public class rightObs : MonoBehaviour {      public float moveSpeed;     public int x;     public int y;        void Update()     {         int b = -3;          transform.position =            new Vector3(b * Mathf.PingPong(Time.time, 3), transform.position.y, transform.position.z);       }    }  